export declare enum MessageKind {
    Log = 0,
    Show = 1,
}
export declare class LanguageWorkerError extends Error {
    private _messageKind;
    message: string;
    constructor(message: string, messageKind: MessageKind);
    messageKind: MessageKind;
}
