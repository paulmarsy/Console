import { Message } from './messages';
export interface ICallback {
    (data: Message): void;
}
export declare class MessageReader {
    private readable;
    private callback;
    private buffer;
    private nextMessageLength;
    constructor(readable: NodeJS.ReadableStream, callback: ICallback, encoding?: string);
    private onData(data);
}
